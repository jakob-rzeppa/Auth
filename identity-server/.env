POSTGRES_USER=identity_server
POSTGRES_PASSWORD=change-me
POSTGRES_DB=identity_server

DATABASE_URL=postgres://identity_server:change-me@identity-server-db:5432/identity_server
# Only used for dev mode
LOCAL_POSTGRES_URL=postgres://identity_server:change-me@127.0.0.1:5432/identity_server

DATABASE_PEPPER=change-me

# The length of temporary passwords - this should be increased in prod.
TEMPORARY_PASSWORD_LENGTH=4

# Port the app binds to inside its container.
APP_PORT=8080

# Host ports. Only published by docker-compose.dev.yml.
APP_HOST_PORT=8080
DB_HOST_PORT=5432
