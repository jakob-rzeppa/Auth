[package]
name = "identity-server"
version = "0.1.0"
edition = "2024"

[dependencies]
dotenv = "0.15.0"
fnmock = "0.1.0"
sqlx = { version = "0.9.0", features = ["postgres", "runtime-tokio", "macros"] }
uuid = "1.26.0"
